Linear Search
arr = [10, 20, 30, 40, 50]
target = 40

def linearSearch(target, arr):
    for i in range(len(arr)):
        if arr[i] == target:
            return i
    return -1

indx = linearSearch(target, arr)  # Call the correct function and get the result
if indx == -1:
    print("Target not found")
else:
    print("Found at index", indx)
